spring.application.name=product

# Products are stored as JSON text in this file instead of a database.
# Path is relative to where the app is run from.
product.storage.file=data/products.json

server.port=8080
